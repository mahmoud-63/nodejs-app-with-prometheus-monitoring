apiVersion: v1
kind: Service
metadata:
  name: nodejs-svc
  annotations:
    prometheus.io/scrape: 'true'
spec:
  type: NodePort
  selector:
    app: nodejs
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 32000
      name: nodejs
